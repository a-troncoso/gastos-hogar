import { useState, useCallback } from "react"

export const useApp = () => {
  // const [isKeyboardVisible, setIsKeyboardVisible] = useState(false)

  // const setIsKeyboardDisplayed = useCallback(isdisplayed =>
  //   setIsKeyboardVisible(isdisplayed)
  // , [])

  return {}
  // return { isKeyboardVisible, setIsKeyboardDisplayed }
}
